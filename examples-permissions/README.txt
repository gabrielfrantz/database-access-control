ATENÇÃO: Para utilizar os exemplos de permissões abaixo, copie TODO o conteúdo do arquivo JSON desejado e cole no campo de input de permissões do workflow, mantendo a formatação.

Os arquivos disponíveis são:
- schema_full_access.json: Permissões para todas as tabelas de um schema.
- schema_with_granular_tables.json: Permissões gerais no schema, com granularidade em algumas tabelas.
- multiple_schemas.json: Permissões em múltiplos schemas, com e sem granularidade.

Certifique-se de colar o JSON exatamente como está no arquivo, sem adicionar ou remover colchetes, chaves ou aspas. 